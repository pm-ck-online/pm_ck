# ==============================================================================
# pm_ck — Phan mem theo doi & mo phong giao dich CK Viet Nam
# Requirements co ban cho giai doan khoi tao khung du an
# ==============================================================================

# --- Xu ly du lieu ---
pandas>=2.2
numpy>=1.26
scipy>=1.11  # can cho core/stock_character_classifier.py (percentileofscore)

# --- Chi bao ky thuat ---
pandas-ta>=0.3.14b0

# --- Cau hinh & tien ich ---
PyYAML>=6.0
python-dotenv>=1.0

# --- Luu tru du lieu ---
SQLAlchemy>=2.0

# --- Goi API / thu thap du lieu ---
requests>=2.31

# --- Du lieu chung khoan Viet Nam that (bat buoc khi data_source.adapter
#     trong config.yaml la "vnstock" — truoc day chi cai tay tren may ca
#     nhan, CHUA tung khai bao o day, khien may moi/GitHub Actions khong
#     tu cai duoc, gay loi "Chua cai dat thu vien 'vnstock'") ---
vnstock

# --- Thong bao Telegram ---
python-telegram-bot>=21.0

# --- Giao dien dashboard ---
streamlit>=1.42

# --- Xac thuc dang nhap (st.login) — bat buoc de dashboard ho tro
#     dang nhap bang Google truoc khi xem noi dung ---
Authlib>=1.3.2

# --- Bieu do (dung trong backtest_engine) ---
matplotlib>=3.8

# --- Bieu do tuong tac (nen Nhat, khoi luong) cho dashboard ---
plotly>=5.20

# --- PostgreSQL/Supabase (tuy chon — chi can khi dung chung du lieu
#     nhieu may qua Supabase; khong can neu chi dung SQLite cuc bo) ---
psycopg2-binary>=2.9
statsmodels>=0.14

# --- Kiem thu ---
pytest>=8.0
pytest-mock>=3.14
